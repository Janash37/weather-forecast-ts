import { FC } from "react";

export const InfoPanel: FC = () => {
  return (
    <div>
      <p>
        This is the info panel, where weather data will be shown once I can get
        the bloody thing to work.
      </p>
    </div>
  );
};
